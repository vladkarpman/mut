# Demo test for verifying the CLI run command pipeline
config:
  app: com.android.calculator2

setup:
  - launch_app

steps:
  - tap: "5"
  - tap: "+"
  - tap: "3"
  - tap: "="

teardown:
  - terminate_app
